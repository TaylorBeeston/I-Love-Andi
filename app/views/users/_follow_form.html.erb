<% unless current_user?(u) %>
  <div class="follow_form">
    <% if current_user.following?(u) %>
      <%= render(partial: 'users/unfollow', locals: { u: u }) %>
    <% else %>
      <%= render(partial: 'users/follow', locals: { u: u }) %>
    <% end %>
  </div>
<% end %>
